import React from "react";

export default function CaoNgoAnhVyProfile() {
  const avatarUrl = "https://images.unsplash.com/photo-1544005313-94ddf0286df2?w=800&q=80&auto=format&fit=crop";

  const hobbies = ["Hát", "Vẽ", "Du lịch"];
  const strengths = [
    "Giỏi Toán, Văn, Anh và Vật lý",
    "Có tư duy tài chính tốt",
    "Hoạt bát, thích giao tiếp với mọi người",
  ];
  const weaknesses = ["Hay cầu toàn", "Dễ nhận quá nhiều việc"];
  const slogan =
    "Sự tò mò nuôi dưỡng sáng tạo — hãy cùng học hỏi, sáng tạo và khám phá.";

  return (
    <div className="min-h-screen bg-gradient-to-tr from-white to-slate-50 flex items-center justify-center p-6">
      <div className="max-w-4xl w-full bg-white shadow-xl rounded-2xl overflow-hidden grid grid-cols-1 md:grid-cols-2">
        {/* Left */}
        <div className="p-8 flex flex-col justify-center gap-6">
          <div>
            <h1 className="text-3xl md:text-4xl font-extrabold">Cao Ngô Anh Vy</h1>
            <p className="mt-1 text-sm text-gray-500">Học sinh · Định hướng Nhân sự & Đối ngoại</p>
          </div>

          <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div>
              <h3 className="text-sm font-semibold text-gray-700">Sở thích</h3>
              <ul className="mt-2 space-y-1 text-sm text-gray-600 list-disc list-inside">
                {hobbies.map((h) => (
                  <li key={h}>{h}</li>
                ))}
              </ul>
            </div>

            <div>
              <h3 className="text-sm font-semibold text-gray-700">Điểm mạnh</h3>
              <ul className="mt-2 space-y-1 text-sm text-gray-600 list-disc list-inside">
                {strengths.map((s) => (
                  <li key={s}>{s}</li>
                ))}
              </ul>
            </div>
          </div>

          <div>
            <h3 className="text-sm font-semibold text-gray-700">Điểm yếu</h3>
            <ul className="mt-2 space-y-1 text-sm text-amber-700 list-disc list-inside">
              {weaknesses.map((w) => (
                <li key={w}>{w}</li>
              ))}
            </ul>
          </div>

          <div className="mt-2">
            <span className="inline-block bg-indigo-50 text-indigo-700 px-3 py-2 rounded-full text-sm font-medium">Slogan</span>
            <p className="mt-3 text-lg md:text-xl font-semibold">“{slogan}”</p>
          </div>
        </div>

        {/* Right: Avatar */}
        <div className="relative bg-gradient-to-b from-indigo-50 to-white flex items-center justify-center p-8">
          <div className="w-60 h-60 md:w-72 md:h-72 rounded-3xl shadow-2xl overflow-hidden ring-4 ring-white">
            <img src={avatarUrl} alt="Ảnh đại diện" className="w-full h-full object-cover" />
          </div>
          <div className="absolute bottom-6 right-6 bg-white/90 backdrop-blur-md px-3 py-2 rounded-full shadow-md text-sm font-medium">
            Ho Chi Minh City · ✨
          </div>
        </div>
      </div>
    </div>
  );
}
