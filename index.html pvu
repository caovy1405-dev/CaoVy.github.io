import React from 'react';

// Usage: replace AVATAR_URL with your avatar image URL
// Drop this component into a React + Tailwind project (e.g. Next.js). It uses Tailwind for layout + small inline CSS for the ice animation.

export default function NguyenPhiVuIntro() {
  const AVATAR_URL = 'https://images.unsplash.com/photo-1544005313-94ddf0286df2?w=800&q=80';

  return (
    <main className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-900 via-indigo-900 to-black px-6 py-12">
      <div className="max-w-5xl w-full bg-[linear-gradient(135deg,rgba(255,255,255,0.03),rgba(255,255,255,0.01))] rounded-2xl shadow-2xl relative overflow-hidden grid grid-cols-1 md:grid-cols-2">

        {/* Left: Info */}
        <section className="p-10 md:pr-16 flex flex-col justify-center z-10">
          <div className="mb-4">
            <h1 className="text-4xl md:text-5xl font-extrabold tracking-tight text-white">Nguyễn Phi Vũ</h1>
            <p className="mt-2 text-indigo-200 font-medium">Slogan: <span className="text-white">"Băng giá trong tim — nhiệt huyết trong hành động"</span></p>
          </div>

          <div className="mt-6 grid grid-cols-1 sm:grid-cols-2 gap-4 text-indigo-100">
            <div className="p-4 bg-white/3 rounded-xl backdrop-blur-sm border border-white/6">
              <h3 className="text-sm font-semibold">Sở thích</h3>
              <ul className="mt-2 text-sm leading-relaxed list-disc list-inside">
                <li>Chơi nhạc, guitar acoustic</li>
                <li>Du lịch khám phá vùng lạnh/ núi</li>
                <li>Thiết kế đồ họa và chụp ảnh</li>
              </ul>
            </div>

            <div className="p-4 bg-white/3 rounded-xl backdrop-blur-sm border border-white/6">
              <h3 className="text-sm font-semibold">Điểm mạnh</h3>
              <ul className="mt-2 text-sm leading-relaxed list-disc list-inside">
                <li>Tư duy logic, giải quyết vấn đề</li>
                <li>Kỹ năng giao tiếp tốt</li>
                <li>Học nhanh, chịu áp lực tốt</li>
              </ul>
            </div>

            <div className="p-4 bg-white/3 rounded-xl backdrop-blur-sm border border-white/6 sm:col-span-2">
              <h3 className="text-sm font-semibold">Điểm yếu</h3>
              <p className="mt-2 text-sm leading-relaxed">Thỉnh thoảng quá cầu toàn, dễ sa đà vào chi tiết nhỏ. Đang rèn luyện kỹ năng ủy quyền và quản lý thời gian tốt hơn.</p>
            </div>
          </div>

          <div className="mt-8">
            <a href="#contact" className="inline-block px-6 py-3 rounded-full text-sm font-semibold border border-white/10 bg-gradient-to-r from-cyan-400/10 to-indigo-300/5 hover:scale-105 transform transition">Liên hệ / Xem thêm</a>
          </div>
        </section>

        {/* Right: Avatar with ice effect */}
        <aside className="relative p-6 md:p-10 flex items-center justify-center z-10">
          <div className="relative w-64 h-64 md:w-80 md:h-80 rounded-xl overflow-hidden shadow-inner border border-white/10">
            {/* Frosted overlay + avatar */}
            <img src={AVATAR_URL} alt="Nguyễn Phi Vũ avatar" className="w-full h-full object-cover" />

            {/* Ice crack / shard layers */}
            <div className="absolute inset-0 pointer-events-none">
              <div className="absolute -left-20 -top-20 w-96 h-96 blur-2xl opacity-40 animate-iceWave" />
              <div className="absolute -right-16 -bottom-16 w-72 h-72 blur-3xl opacity-30 animate-iceDrift" />

              {/* subtle frost texture */}
